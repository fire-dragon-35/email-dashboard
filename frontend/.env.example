# WebSocket URL of the IMAP relay (see ../relay/). ws:// for local dev
# only — wss:// (TLS) is mandatory anywhere real, since your app password
# transits this connection.
VITE_RELAY_URL=ws://localhost:8080
