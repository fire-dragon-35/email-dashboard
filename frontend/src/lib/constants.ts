// Real IMAP has no categorization decided yet (see agent.md's "Not yet
// decided") — every message lands in this one placeholder bucket until
// that design happens.
export const ALL_MAIL_CATEGORY_ID = 'all';
